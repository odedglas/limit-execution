{
  "private": true,
  "workspaces": [
    "limit-execution",
    "examples"
  ],
  "scripts": {
    "lint": "eslint .",
    "test": "cd limit-execution && jest",
    "build": "cd limit-execution && npm run build",
    "publish": "cd limit-execution && npm publish"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.8",
    "@typescript-eslint/eslint-plugin": "^5.9.1",
    "@typescript-eslint/parser": "^5.9.1",
    "eslint": "^8.6.0",
    "eslint-plugin-import": "^2.25.4"
  }
}
